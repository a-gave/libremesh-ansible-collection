node:
{% for target in groups['libremesh_devices'] %}
  - targets: ['{{ ip_network }}.{{ hostvars[target].ipv4_host }}:9090']
    labels:
      host: '{{ hostvars[target].hostname }}'
      lime_mac: '{{ hostvars[target].lime_mac }}'
{% endfor %}